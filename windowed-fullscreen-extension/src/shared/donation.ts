/**
 * Shared Donation_Link destination (Requirement 8).
 *
 * The external donation page is presented from multiple surfaces (the Popup and
 * the Options_Page). Centralizing the URL here keeps those surfaces in sync and
 * gives a single place to update the project's donation URL.
 */
export const DONATION_URL = "https://ko-fi.com/rohittiger";
